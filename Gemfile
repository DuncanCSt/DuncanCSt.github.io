source "https://rubygems.org"

# Matches the exact Jekyll + plugin versions GitHub Pages runs on its servers,
# so a local build reflects what gets published.
gem "github-pages", group: :jekyll_plugins

# Faster file-watching on macOS for `jekyll serve`.
gem "webrick"

group :jekyll_plugins do
  # add plugins here if needed
end
